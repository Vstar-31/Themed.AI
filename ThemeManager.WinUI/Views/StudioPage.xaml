<Page
    x:Class="ThemeManager.WinUI.Views.StudioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Padding="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="DESKTOP STUDIO" FontSize="12" FontWeight="SemiBold" Opacity="0.65" />
                    <TextBlock Text="Build your world" FontSize="36" FontWeight="Bold" Margin="0,4,0,0" />
                    <TextBlock Text="Compose wallpapers, widgets, atmosphere and Windows personality as one living scene." FontSize="14" Opacity="0.72" Margin="0,6,0,0" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Surprise me" Click="SurpriseMe_Click" Margin="0,0,8,0" />
                    <Button Content="Generate world" Click="GenerateWorld_Click" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.35*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="18" Padding="18" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="0,0,16,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="WORLDS" FontWeight="SemiBold" Opacity="0.7" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding ElementName=ScenesList, Path=Items.Count, Mode=OneWay}" FontSize="11" Opacity="0.45" VerticalAlignment="Center" />
                        </Grid>
                        <ListView x:Name="ScenesList" Grid.Row="1" Margin="0,12,0,0" SelectionChanged="ScenesList_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="8,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.65" TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Widgets.Count}" Opacity="0.6" />
                                            <TextBlock Text="widgets" FontSize="10" Opacity="0.4" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Border Grid.Column="1" CornerRadius="22" Padding="28" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <TextBlock Text="ACTIVE WORLD" FontSize="11" FontWeight="SemiBold" Opacity="0.6" />
                            <TextBlock x:Name="ActiveSceneName" Text="No world selected" FontSize="30" FontWeight="Bold" Margin="0,4,0,0" />
                            <TextBlock x:Name="ActiveSceneMeta" Text="Select a world to preview its composition." FontSize="13" Opacity="0.65" Margin="0,4,0,0" TextWrapping="Wrap" />
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" Margin="0,24,0,18">
                            <StackPanel x:Name="SceneDetails" Spacing="10" />
                        </ScrollViewer>

                        <Border Grid.Row="2" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="14" Padding="16" Margin="0,0,0,16">
                            <StackPanel Spacing="12">
                                <StackPanel Spacing="3">
                                    <TextBlock Text="THEMED WALLPAPER" FontSize="11" FontWeight="SemiBold" Opacity="0.65" />
                                    <TextBlock Text="Generate a wallpaper from the active theme palette and bind it to this desktop world. Everything runs locally and is free." FontSize="12" Opacity="0.65" TextWrapping="Wrap" />
                                </StackPanel>

                                <Grid ColumnSpacing="10" RowSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <ComboBox Grid.Column="0" x:Name="WallpaperStyleCombo" Header="Style" ItemsSource="{x:Bind WallpaperStyles}" SelectedIndex="0" />
                                    <ComboBox Grid.Column="1" x:Name="WallpaperSizeCombo" Header="Resolution" ItemsSource="{x:Bind WallpaperSizes}" SelectedIndex="0" />

                                    <TextBox Grid.Row="1" Grid.Column="0" x:Name="WallpaperNameBox" Header="Name" PlaceholderText="World wallpaper" />
                                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Spacing="8">
                                        <Button Content="Generate" Click="GenerateWallpaper_Click" />
                                        <Button Content="Generate + Apply" Click="GenerateAndApplyWallpaper_Click" />
                                    </StackPanel>
                                </Grid>

                                <Button Content="Apply last generated wallpaper" Click="ApplyLastWallpaper_Click" HorizontalAlignment="Left" />
                                <TextBlock x:Name="WallpaperStatusText" Text="Ready — generated wallpapers follow the active theme palette." FontSize="11" Opacity="0.6" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Grid Grid.Row="3" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock x:Name="ApplyStatus" Text="Ready" FontSize="12" Opacity="0.6" />
                                <TextBlock Text="A world restores its saved widget layout and wallpaper together." FontSize="12" Opacity="0.45" Margin="0,3,0,0" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="Save current layout" Click="SaveCurrentLayout_Click" />
                            <Button Grid.Column="2" Content="Duplicate" Click="DuplicateWorld_Click" />
                            <Button Grid.Column="3" Content="Delete" Style="{StaticResource DangerButtonStyle}" Click="DeleteWorld_Click" />
                            <Button Grid.Column="4" Content="Apply to desktop" Click="ApplyScene_Click" Padding="20,10" />
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <TextBlock Grid.Row="2" Text="THEMED.AI  •  Desktop worlds are the new skins" HorizontalAlignment="Center" Opacity="0.4" FontSize="11" Margin="0,18,0,0" />
        </Grid>
    </ScrollViewer>
</Page>
